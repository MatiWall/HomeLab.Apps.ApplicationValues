# my-react-app/values.yaml

global:
  name: mwstack-fittracker-frontend
deployment:
  replicaCount: 1
  image:
    repository: docker-registry.mw.local/fittracker-frontend
    tag: 

  service:
    name: fittracker-frontend
    port: 80
  ingress:
    enabled: true
    host: fittracker.mw.local
    path: /
    tls:
      enable: true
